# TODO

## Stripe
- create MembershipSuccess page
- create MembershipCancel page
- add `/membership/success` and `/membership/cancel` routes
- update FAQ text
- set `VITE_API_BASE_URL` in `.env` once backend is deployed

## Backend
- deploy backend to Railway/Render
- set all env vars
- configure Stripe webhook endpoint
